rules_version = '2';
service cloud.firestore {
  // DEV-ONLY permissive rules
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // WARNING: open for development and testing only
    }
  }
}
